# Todo Skill

This is the development repo for the `/todo` Claude Code skill. The installed (live) copy lives at `~/.claude/skills/todo/`.

## Sync After Changes

After editing `SKILL.md` or any file in `templates/`, always sync to the installed location by running `./install.sh`. Do this automatically — don't wait for the user to ask.
